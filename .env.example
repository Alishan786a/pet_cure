# Public site URL (no trailing slash). Used for canonical, Open Graph image URLs, and JSON-LD.
# VITE_SITE_URL=https://your-domain.com

# Optional: override EmailJS (defaults are set in src/emailjsConfig.ts).
# VITE_EMAILJS_SERVICE_ID=
# VITE_EMAILJS_TEMPLATE_ID=
# VITE_EMAILJS_PUBLIC_KEY=
